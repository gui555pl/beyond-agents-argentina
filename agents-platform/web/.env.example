# URL pública do backend Beyond Agents (sem trailing slash).
# Em dev local, deixe vazio (o vite.config.ts faz proxy para localhost:4001).
# Em prod (Vercel), aponte para a URL do backend Fly.io.
#
# Exemplo:
#   VITE_API_URL=https://beyond-agents-api.fly.dev
VITE_API_URL=
